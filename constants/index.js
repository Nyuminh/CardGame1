// Các hằng số dùng chung cho toàn project

const TOKEN_EXPIRES_IN = '15m';
const REFRESH_TOKEN_COOKIE = 'refreshToken';
const DEFAULT_AVATAR = '/images/default-avatar.png';

module.exports = {
  TOKEN_EXPIRES_IN,
  REFRESH_TOKEN_COOKIE,
  DEFAULT_AVATAR
};
